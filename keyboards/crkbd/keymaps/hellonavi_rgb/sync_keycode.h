// sync_keycode.h
#ifndef SYNC_KEYCODE_H
#define SYNC_KEYCODE_H

#include <stdint.h> // Include this header for uint16_t

typedef struct {
    uint16_t keycode;
} sync_keycode_t;

#endif // SYNC_KEYCODE_H
